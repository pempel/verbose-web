"use strict";

var React = require("react");
var CardSearchForm = require("components/CardSearchForm");

var AddCardPage = React.createClass({
  render: function() {
    return (
      <CardSearchForm/>
    );
  }
});

module.exports = AddCardPage;
